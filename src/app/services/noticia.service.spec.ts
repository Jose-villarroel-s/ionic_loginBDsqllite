import { TestBed } from '@angular/core/testing';

import { NoticiaService } from './noticia.service';



describe('Noticias', () => {
  it('should create an instance', () => {
    expect(new NoticiaService()).toBeTruthy();
  });
});
